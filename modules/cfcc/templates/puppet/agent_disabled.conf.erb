# WARNING: This file is managed by Puppet.
# Local changes may not (and probably won't) be saved.
[main]
server=<%=@puppet_master %>
autoflush=true
environment=production
runinterval=<%=@run_interval %>
